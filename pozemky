using System;

namespace fisk_pozemky
{
    class Program
    {
        static void Main(string[] args)
        {
            int pocetZadani = 0, pocetZvirat = 1;
            //int[,] listOfAnimals = new int[pocetZvirat,2];

            Console.WriteLine("Zadejte seznam zvířat...");

            int.TryParse(Console.ReadLine(), out pocetZadani);
            int[,] listOfAnimals = new int[200, 2];

            for (int i = 0; i < pocetZvirat; i++) {
                int.TryParse(Console.ReadLine(), out pocetZvirat);
                String[,] listOfAnimalsFor = new String[pocetZvirat, 2];
                for (int j = 1; j < pocetZvirat; j++) {
                    listOfAnimalsFor[j, 1] = Console.Read();


                }
            }
            for (int i = 0; i < pocetZvirat; i++) {
                Console.WriteLine(listOfAnimals[i, 0]);
                Console.WriteLine(listOfAnimals[i, 1]);
            }
        }
        static int ConvertListToSpace(int x) {
            return 0;
        }
    }
}

